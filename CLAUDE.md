# CLAUDE.md — Syndicate IMS

## Current state — KEEP THIS UPDATED
This is the only section that goes stale. Everything below it is stable intent.

- **OTP (email 2FA + checkout confirmation): DONE, both halves the client asked for.** Client confirmed scope via 3 questions: pickup + prepaid only (no COD/delivery/50% DP — stays out of scope), OTP on **both** login and checkout, and ID verification as a separate manual photo-upload+admin-review feature (not started yet, tracked separately below). New `otp_codes` table + `App\Models\OtpCode` (`PURPOSE_LOGIN`/`PURPOSE_CHECKOUT` constants, not enums — PHP 8.0) — 6-digit codes, **sha256** hashed (deliberately not bcrypt: these are short-lived and rate-limited, bcrypt's cost factor buys nothing here and slows down `attempt()`), 10-minute expiry, 5 max attempts, `hash_equals()` for timing-safe comparison, one live code per user+purpose at a time (`issue()` invalidates any prior unused code for the same user+purpose). `App\Mail\OtpCodeMail` + `resources/views/emails/otp-code.blade.php` (dark/volt themed to match the brand). **Login**: `Auth\AuthenticatedSessionController::store()` now authenticates credentials, immediately logs back out, emails a code, and redirects to `login.otp.create` instead of completing the session — `Auth\LoginOtpController` (`create`/`store`/`resend`) is what actually calls `Auth::login()` once the code checks out. **Checkout**: `Shop\CheckoutController::store()` no longer creates an `Order` directly — it validates the form, stashes it in session (`pending_checkout`), emails a code, and redirects to `checkout.otp.create`; **all the actual Order/OrderItem creation logic (stock re-check, snapshot, colour customization) now lives in `otpStore()`**, moved verbatim from the old `store()`. This is a real behavioral seam: anything that used to assume `POST /checkout` immediately produces an `Order` no longer holds. **Ripple effect, now fully fixed**: every existing test that posted to `checkout.store` and expected an `Order::firstOrFail()` right after needed a second step — `tests/Concerns/CompletesCheckoutOtp.php` (new shared trait, `Mail::assertSent(OtpCodeMail::class, ...)` to read the code back off the faked Mailable, then posts it to `checkout.otp.store`) is the fix, applied to `CheckoutTest`, `PayMongoPaymentTest`'s `orderAwaitingPayment()` helper, `OrderCancellationTest`'s own copy of that helper, and `OrderAddressTest` (which had no shared helper — each test call site got its own `Mail::fake()` + trait call). 221 tests passing, `npm run build` clean. **Frontend**: `Components/Storefront/OtpForm.jsx` (shared code-entry + resend form) backs both `Pages/Auth/VerifyLoginOtp.jsx` and `Pages/Storefront/CheckoutOtp.jsx`. **Not yet done**: ID verification (photo upload + admin review) — confirmed scope, zero code written yet.

- **Toast notifications: DONE, replacing every static flash banner app-wide.** `resources/js/utils/useFlashToasts.js` (shared hook) + `Components/Admin/ToastStack.jsx` / `Components/Storefront/ToastStack.jsx` (skinned presentation, mounted in `AdminLayout`, `StorefrontLayout`, and `StorefrontAuthLayout` — the auth pages were on a separate shell with zero notice handling before this). **Pure frontend change** — `flash.success` and `errors` were already shared on every Inertia response; nothing in a controller changed. Two real bugs fixed as a result: `Admin\UserController`'s two self-guard rejections had **zero visible feedback at all** before this (silently no-op'd), and the storefront's old banner was hardcoded to exactly `errors.cart`/`errors.payment`, silently dropping anything else. **Two non-obvious things worth knowing if you touch this:** (1) `errors` mixes real FormRequest field validation (`email`, `sku`, `price`...) — which every form already shows inline and must NOT also toast — with one-off `withErrors(['word' => 'A full sentence.'])` action messages that have no field UI. The hook only toasts an explicit allowlist (`ACTION_ERROR_KEYS` in `useFlashToasts.js`: `cart`, `payment`, `user`, `variant`, `status`) — add a key there when a new controller introduces the same pattern, never remove the allowlist entirely. (2) **Inertia fires a SEPARATE `'error'` global event, not `'success'`, whenever a response's `errors` bag is non-empty** — confirmed by reading `@inertiajs/core`'s source, not assumed from the name. A hook that only listens to `router.on('success', ...)` will never see a single error message; this one listens to both, with different payload shapes (`event.detail.page.props` vs. `event.detail.errors`, the latter already unwrapped). Verified in a real browser: admin success (deactivate a user, remove a variant) and the previously-invisible error (delete a variant with order history) both render correctly; storefront in both themes, including 2 stacked toasts; a bad-login/rate-limited field error stays inline-only with no duplicate toast, confirming the allowlist works in the direction that matters.

- **Admin panel: DONE, retheme to the "Control Room" volt/dark look — every page, not just the Dashboard.** Client asked to match the admin to the storefront's look; a first pass kept the admin's own blue accent (dark + blue), then the client approved a bolder mockup (Artifact, dummy data) and asked for it applied everywhere with real data. **`volt` and `ink` are now intentionally shared tokens between storefront and admin** — same hex values, still a completely different execution (admin: Oswald/Jakarta not Anton, sharp `rounded-md` 6px corners not loud full-bleed photography, dense restrained tables not a brand voice; zero storefront JSX components imported, that's the real separation, not the hex). The old admin-only `brand` (blue) token is gone — confirmed fully unused before deletion. Two small new tokens, `chart-lime`/`chart-blue`, exist only for the Apparel-vs-Skateboard donut — pre-validated with the dataviz skill's OKLCH/CVD checker, since full-brightness `volt-500` fails as a categorical chart mark against a dark surface (too light); `volt-500` itself stays reserved for buttons/active-states/glow and the sales chart's own line (a solo series, no CVD pairing needed). **No light/dark toggle was added** — the admin was already dark-only with zero theme-switching plumbing, and nothing asked for one. **No fabricated data** — the Dashboard's "Revenue Today" card gets one real new backend query (`revenue_yesterday_centavos`) to power a real vs-yesterday trend badge and a real 7-day sparkline (sliced from data already fetched for the chart); every other stat card stays a plain value, since a snapshot count like "Products: 39" has no real trend to show. When there's no real yesterday figure (`hasTrend` false), the sparkline renders neutral-grey rather than defaulting to red — an earlier version silently implied a decline that was never computed. `AdminLayout.jsx` gained a real live clock (`Components/Admin/Clock.jsx`, `Intl.DateTimeFormat` in `Asia/Manila`, matching `APP_TIMEZONE`) and a topbar eyebrow label naming the current sidebar group (Overview/Catalogue/Sales/Admin), plus a glassy `backdrop-blur` sticky topbar; "View Storefront" moved from the sidebar footer into a bold `bg-volt-500 text-ink-900` topbar CTA. Metric cards on the Dashboard gained a bordered icon box each (new hand-copied icons: `BanknotesIcon`, `ClockIcon`, `TruckIcon`, `TagIcon`, `CubeIcon`, `ExclamationTriangleIcon`, `XCircleIcon` in `icons.jsx`) and a faint SVG grid-texture + volt glow on the primary card — closing a real fidelity gap against the approved mockup, where the first implementation pass had shipped the cards as bare text. Recent Orders on the Dashboard is now a real `<table>` (Order/Customer/Items/Total/Status/Placed/View) matching the mockup, not a `<ul>` list — `DashboardController` now also returns `items_count` per order. `OrderStatusBadge` gained a small colour-matched dot before the label, same as every other status pill in the mockup. Verified: `npm run build` clean, all 162 tests still passing, and a full temporary-Puppeteer real-browser pass (login, Dashboard, Products index/create/edit, Orders index/show, Skate Components index/edit, Users index) as both `staff` and `admin` — confirmed contrast, confirmed the donut/chart/sparkline render with real data, confirmed role-gating on `/admin/users` still 403s a staff account correctly. **Follow-up, same day: closed remaining fidelity gaps the client caught by direct screenshot comparison against the mockup.** Sidebar wordmark is now two lines (`SYNDICATE` / `ADMIN`, stacked) matching the mockup's brand-block structure instead of one inline line. Sales Trend gained the mockup's **"View as table" toggle** (`RevenueTrendTable`, local `showTrendTable` state — same `revenueTrend` data, no new query) as an alternative to the chart. The Apparel-vs-Skateboard donut card gained a **compact "Needs restocking" widget** (`CompactRestockList`, top 4 of the real `lowStockItems`) directly beneath the donut, matching the mockup's layout — the full, complete low-stock list further down the page is unchanged and stays the actionable one (Objective 4 needs every row, not just 4; the compact widget is a glance, not a replacement). **One deliberate non-match, called out rather than silently skipped**: the mockup's other three metric cards (Revenue 30 Days, Decks Sold, Low Stock Alerts) all carry trend badges — that's dummy data. The real dashboard only puts a trend badge on Revenue Today, the one card with a real day-over-day figure to show; the other cards render as plain values rather than inventing numbers to look busier. Re-verified: clean build, 162 tests, one more temporary-Puppeteer pass confirming the sidebar wordmark, the table toggle (both directions), and the donut's restock widget all render and interact correctly.

- **Admin panel: DONE, real light/dark toggle added.** Client asked for a light-mode switch beside the topbar clock, matching the mockup's icon button — a genuine reversal of the earlier "admin is dark-only, nothing asked for a toggle" call, now built for real across every admin page. **Deliberately a separate mechanism from the storefront's own toggle** (`useTheme`/`data-theme`/`syndicate-theme` on `<html>`) — new `resources/js/utils/useAdminTheme.js` (`data-admin-theme`, `syndicate-admin-theme` localStorage key, `syndicate-admin-theme-change` CustomEvent, same DOM-pub/sub shape as the storefront's hook) and a new Tailwind variant `admin-light:` → `[data-admin-theme="light"] &`. Switching one panel's theme must never flip the other's, and they don't even share the DOM node carrying the attribute. **`data-admin-theme` lives on a thin unstyled wrapper div, one level above `AdminLayout`'s actual styled root** — not on that styled root itself and not on `<html>`. A real bug surfaced during verification: `admin-light:` compiles to a *descendant* selector (`[data-admin-theme="light"] .foo`), which can never match the same element that carries the attribute — so the first attempt (attribute + `admin-light:bg-paper` on one div) silently failed, leaving the page background solid black behind visibly-white cards. Fixed by moving the attribute up to a wrapping div; confirmed via computed-style inspection (`getComputedStyle(...).backgroundColor`), not just eyeballing a screenshot, since a subtly-wrong background is easy to miss at a glance. **No pre-paint blade script, unlike the storefront's** — the admin has no boot splash (CLAUDE.md already documents "the admin gets no brand splash"), so there is nothing painted before React mounts that a FOUC-prevention script would need to fix; React's first render already carries the right attribute, read synchronously from `localStorage`. Every admin surface got a real `admin-light:` pair, following the same rules the storefront's own `light:` pass established: `bg-ink-900`→`bg-white`, `border/divide-white/N`→`border/divide-ink-900/N`, `text-white`→`text-ink-900`, `text-white/N`→a same-ballpark-but-~10-15-points-hotter `ink-900/N` (light backgrounds dilute dark foregrounds faster); `text-volt-500` used as **text** (links, the topbar eyebrow, active nav icon) pairs to `volt-800` (raw volt-500 is ~1.2:1 on white, same exception the storefront already carries) — but `bg-volt-500` used as a **fill** (PrimaryButton, active FilterTab/Pagination pill, the View Storefront CTA) stays completely unpaired in both themes, same "fills stay literal" rule storefront buttons follow. Semantic status colors (`StockBadge`, `OrderStatusBadge`'s paid/awaiting/fulfilled/failed, the amber/blue order-queue summary cards) are also unpaired — already legible on both surfaces as saturated text on a 10%-tint background, same precedent `OrderStatusBadge`'s own neutral/pending/cancelled tones set by contrast (those two *did* need pairing, being plain white/[0.06]-on-dark). **Two new light-only chart tokens**, `chart-lime-light`/`chart-blue-light` (`#5C7A00`/`#2A78D6`) — a dark-surface-safe categorical mark isn't automatically light-surface-safe (different lightness band), so these are a *separately* dataviz-skill-validated pair against the light surface, not the dark pair reused; the Dashboard's donut switches between the two pairs via plain `admin-light:stroke-chart-blue-light` Tailwind classes. **Recharts is the one exception that can't be a CSS pair** — it reads literal hex props at render time, so `Dashboard.jsx`'s `RevenueTrendChart`/`Sparkline` take a `theme` prop from `useAdminTheme()` and pick from a `CHART_COLORS` map (light stroke reuses `volt-800` for the same reason storefront text does). Root `Components/{TextInput,InputLabel,InputError}.jsx` (admin-only, shared with Products Create/Edit) got the same treatment. **11 page-level files** (`Products/{Index,Create,Edit}.jsx` + 3 Partials, `SkateboardComponents/{Index,Edit}.jsx`, `Orders/{Index,Show}.jsx`, `Users/Index.jsx`) were paired in bulk by a subagent working from the shared-component files as ground truth, then merged back — confirmed zero `gray-*` leftovers and a clean build both before and after. Verified: `npm run build` clean, 162 tests unaffected (pure className/prop changes, one exception below), a full temporary-Puppeteer pass across every admin page in **both** themes (including a cold-load toggle click and a dark-mode regression check after the wrapper-div fix), and computed-style spot checks (not just screenshots) on the page background and a customer-info block that looked subtly off in a screenshot but proved correctly-colored (`#0b0b0b`) once measured — a reminder that screenshots at reduced size can misread anti-aliased dark grey as tinted blue.

- **Phases 1 (data layer), 2 (RBAC), and a first slice of 3 (admin product management): DONE.** Product CRUD with inline variant management (stock, price override, low-stock threshold) is live at `/admin`, gated by `role:staff`.
- **Phase 4 (storefront: catalogue, cart, checkout): DONE.** `/shop` → `/shop/{slug}` → session cart → `/checkout` → order → payment confirmation. **`InventoryService` is now actually reachable from the running app** — before this it was called by nothing but its own tests.
- **Real PayMongo GCash payment: DONE, alongside the original stub (kept, unmodified).** `Shop\PaymentController::confirm` is still exactly what it always was — local/testing-only, 404s elsewhere, pinned by its own test — and is now dead in the UI (the button just doesn't route to it anymore whenever real keys exist) but deliberately left in the codebase for fast local testing without a real PayMongo round-trip. The real path is new: `Shop\PayMongoController::create` (route `payment.paymongo.create`, `POST /orders/{order}/pay`) creates a PayMongo **Payment Intent**, a **gcash Payment Method**, attaches them (`App\Services\PayMongoClient`, three plain HTTP calls, Basic-auth with the secret key), and sends the customer to PayMongo's real hosted checkout via `Inertia::location()` — **not** a normal `RedirectResponse`; Inertia's own client needs the special 409 + `X-Inertia-Location` response to actually navigate the browser off-site, a real bug caught before ever running it. Starting a payment **never** marks anything paid — only `Webhooks\PayMongoWebhookController` (`POST /webhooks/paymongo`, CSRF-exempt, no auth — see `VerifyCsrfToken::$except`) does that, by calling the exact same `InventoryService::commitForPaidOrder()` the stub always used. **The one place a bug here would be a real security hole**: `App\Services\PayMongoWebhookVerifier` checks the `Paymongo-Signature` header (`t=...,te=...,li=...`, HMAC-SHA256 of `"{t}.{raw body}"`, `te` in test mode/`li` in live) before anything else runs — 8 dedicated unit tests, no DB/HTTP needed. **Two non-obvious things learned the hard way, both from hitting the real API, not docs**: (1) PayMongo's own docs pages were returning empty/404'd schemas repeatedly during this build — the field shapes below were confirmed by actually creating a real test payment intent, authorizing it, and reading the real response back, not assumed. (2) **`payment_intent.succeeded` — the event PayMongo's general docs point to — is not selectable in this account's webhook-creation UI at all.** The only real option is `payment.paid`, whose `data.attributes.data` is a *Payment* resource (`pay_...`), not the intent — but that Payment's own `attributes.payment_intent_id` field carries the intent id (`pi_...`) directly, confirmed by retrieving a real completed payment and reading the field back. The webhook handler matches on that field against `orders.paymongo_payment_intent_id`. Order lookup misses (unknown intent, stale webhook) and irrelevant event types both get a plain `200` back — a webhook endpoint that 4xx's on something it doesn't handle just trains PayMongo to keep retrying forever. `Order.paymongo_payment_intent_id` is saved the instant a payment starts (before the redirect), since the webhook has nothing else to look the order up by and can arrive before the browser even finishes bouncing back. **Local testing needs a public URL** — PayMongo's servers can't reach `localhost`, so local webhook testing runs through an `ngrok` tunnel (free tier) pointed at `php artisan serve`; the webhook is registered against that tunnel's URL in PayMongo's dashboard and re-pointed any time the tunnel restarts (ngrok's free URLs aren't stable across restarts). `config/services.php`'s `paymongo` block (`PAYMONGO_SECRET_KEY`/`PAYMONGO_PUBLIC_KEY`/`PAYMONGO_WEBHOOK_SECRET`) is empty by default — `Shop\OrderController::show()` exposes `payment_configured` (`filled(config('services.paymongo.secret_key'))`) so `Storefront/Orders/Show.jsx` picks the real flow when keys exist and silently falls back to the old stub when they don't, with zero behavior change for anyone who hasn't set keys up yet. 10 feature tests (`Http::fake()` against the real confirmed response shapes, since this project has no live keys checked into any repo) cover the full lifecycle: starting payment, ownership, already-paid, a PayMongo API failure leaving the order untouched, a correctly signed webhook actually marking paid and decrementing stock, and four different ways a webhook can be rejected or safely no-op. Verified for real, once real test keys existed: a live Payment Intent → Payment Method → attach round-trip against `api.paymongo.com` (not mocked, done directly via PowerShell against the real API), and a real browser click-through (Authorize on PayMongo's actual test checkout page, redirected back to this app's `return_url` with `?payment_intent_id=...` exactly as documented) confirmed the checkout-redirect half works end to end. **Not yet confirmed**: an actual webhook delivery landing on a real order through the ngrok tunnel and flipping it to `paid` in this app's own database — that specific full-circle test was queued (webhook registered, `.env` populated) but not completed before this was written. Re-verify that specific loop before relying on it.
- **Admin order management: DONE.** `/admin/orders` (status tabs, search, paginated) and `/admin/orders/{order_number}`. Dashboard now leads with revenue and the two queues that need action, plus the five most recent orders. Objectives 6 and 10.
- **Order status transitions are deliberately narrow** — `Admin\OrderStatusController` allows exactly `paid → fulfilled` and `awaiting_payment → cancelled`. **Cancelling a PAID order is refused**, and there are tests pinning it: that needs stock putting back and money refunding, and `InventoryService` has no inverse on purpose. Build a real refund/restock path with its own audit trail before widening this.
- **Customer self-service order cancellation: DONE.** `Shop\OrderController::cancel` (`POST /orders/{order}/cancel`) mirrors the admin's own `awaiting_payment → cancelled` move exactly — deliberately the *same* narrow transition, not a new one, so the "cancelling a paid order needs a real refund/restock path first" rule above still holds. Owner-only, no staff bypass (staff already have the admin panel's identical transition) — a customer cancelling their own order is a genuinely different action from staff acting on someone else's, even though the code path converges. The "Cancel order" button on `Storefront/Orders/Show.jsx` only renders while `can_cancel` is true. 8 tests.
- **Philippine delivery address, collected at checkout and editable after: DONE.** Five new nullable columns on `orders` (`address_line`, `barangay`, `city`, `province`, `postal_code`) — genuinely optional, since this shop is pickup-first (`notes`' own placeholder copy already said as much) and a customer picking up at a branch has nothing to fill in here. No `country` column: every branch and every customer this system has ever seen is in the Philippines, so the checkout form itself is shaped for PH addressing (barangay included) rather than a generic international one with PH pre-selected. `Shop\OrderController::updateAddress` (`PATCH /orders/{order}/address`) is deliberately **looser** than cancel — no stock or money is at stake, so it stays open any time before the order is fulfilled or cancelled, not just pre-payment — and reuses the same "Change address" / "Add address" modal UI whether one exists yet or not. Admin's own `OrderController::show` exposes the same address fields (read-only) so staff know where to ship. 9 tests.
- **Phase 6a (client's 2026-09-02 feature list, the no-decision-needed half): DONE.** Sales/trend charts on the admin dashboard (Recharts — 30-day revenue area chart, apparel-vs-skateboard split, top 5 products by units sold, all gated on `Order::scopePaid()`), a redesigned `StockBadge` (numeric count + proportion bar, wired into both the Products variant rows and the dashboard's "Needs restocking" list), admin user management (`/admin/users`, `role:admin`-only — role change and activate/deactivate, both via `forceFill()` since `role`/`is_active` are deliberately non-fillable; deactivating actually blocks login, checked in `LoginRequest::authenticate()`; an admin can't change their own role/status), **SkateboardComponent admin CRUD** (`/admin/skateboard-components`, closes the Phase 3 gap below — **edit-only, no create**: `type`/`glb_file`/`mesh_name` are read-only in the UI and never touched by the update payload, because they're load-bearing for the 3D customizer's mesh lookup and a typo would silently break it, pinned by a test), and **cart/customize now require sign-in** — `cart.store`/`customize.store` moved behind `auth` middleware (browsing, the cart page, and the customizer itself stay open to guests; only the add action is gated), with a `?reason=cart` redirect param so the login page can show "Sign in to add items to your cart" instead of a bare bounce. The rest of the client's list (ID verification, COD/pickup decision, OTP, cancel-order requests, 50% DP) is triaged in the Phase 6 roadmap but not started — see that artifact / the conversation, not this file, for the full breakdown.
- **Not yet built:** reports beyond the dashboard charts above, ID verification for buyers (photo upload + admin review — client-confirmed scope, see the OTP entry above). **Resolved, no longer open:** customer-initiated cancel requests (DONE — order-level, see `Shop\OrderController::cancel()`), OTP (DONE, see above), COD/delivery + 50% DP (client confirmed **pickup + prepaid only** — out of scope, not blocked).
- **Skateboard parts: buy separately, and a custom build now shows as ONE cart entry — DONE.** Two client asks, both cart-and-cart-page-only changes (checkout/`OrderItem`/`InventoryService` all untouched — see below). (1) **`/parts`** (new `Shop\PartController`, no auth to browse) — buy a single deck, wheels, trucks, or bolts on its own, reusing the existing `cart.store` route/`AddToCartRequest` verbatim (`type: 'component'`), no new purchase machinery. Decks (14)/Wheels (18) render as real grids; **Trucks/Bolts render as a small fixed "Hardware" pair, not a grid section** — there is exactly one active row of each (`SkateboardComponentSeeder`), so forcing them into the same repeating-card layout would look like a broken empty section. No detail/`Show` page — each `SkateboardComponent` row is already one fully-specified item, no size/color to pick the way apparel has. Nav (`StoreHeader.jsx`) and the footer's previously-unlinked "Decks & completes"/"Wheels & trucks" `<li>`s now point here — that copy predates this page by months. (2) **One cart entry per `/customize` build** — `App\Services\Cart::add()` gained an optional `?string $buildKey`; `CustomizeController::store()` generates one `Str::uuid()` and tags all four `add()` calls with it (display metadata only, stored in the session row, passed through `lines()`). `Cart.jsx` groups lines by `build_key` client-side into one "Custom Board" entry (part names listed underneath, one quantity stepper, one Remove) — `CartController::update()`/`destroy()` now accept a `keys` array as well as a single `key` so the group's stepper/Remove can act on all four lines in one request. **Merge edge case, deliberately not over-engineered:** if a part someone already has standalone in their cart gets re-added as part of a build, the line's `build_key` is left as whatever it already was (first-write-wins) — a later build never silently reclassifies an already-standalone purchase. Checkout still creates one `OrderItem` per line (still 4 for a build) and `InventoryService` still decrements each component's stock independently — confirmed via exploration before writing any code, zero changes needed to either. **Follow-up fix, same day:** the header cart badge (`HandleInertiaRequests`'s shared `cart.count`) was still `Cart::count()` summing raw quantity across all 4 rows — a build showed "4" on the icon while the cart page showed it as one entry, caught by the client from a live screenshot. `count()` now counts each `build_key` group once (first row's quantity, since every member of a build always shares one quantity by construction) — one board now reads "1" everywhere, not just on the cart page. **Follow-up, same week: real part thumbnails.** `/parts` cards used to show a plain "DECK"/"WHEELS" placeholder block — these parts have no product photography (they're 3D-modelled, not shot), so the fix was a real render of the actual baked mesh graphic, not a fake photo. `public/images/parts/` now has 54 PNGs (14 decks + 18 wheel variants + Trucks/Bolts each rendered in all 11 real hardware colours — 22 more — see below for why Trucks/Bolts have no single plain image), generated once via a temporary capture route reusing the real `Components/Customizer/Scene.jsx` (the same component `/customize` and the Home hero use) with one part isolated and `transparentBackground`. `PartController::imageUrl()` derives the path purely from `glb_file`+`mesh_name` (already immutable, load-bearing data — no new column, nothing to desync) and falls back to `null` via `file_exists()` if a file is ever missing, same graceful-fallback shape `Product::image_path` already uses — `PartCard` renders the `<img>` when present, the old placeholder block otherwise. **`Components/Customizer/AutoFitCamera.jsx` is new and stays in the repo** (everything else from the capture harness — the route, controller, page, and generation script — was deleted after use, per this project's standing "build tooling, use it, remove it" rule): it's inert unless a caller explicitly passes `autoFit` on `Scene`, which nothing currently does except that now-deleted harness, so `/customize` and the Home hero are behaviourally unchanged. Kept because it embeds two non-obvious, hard-won fixes documented in its own docblock: (1) sizing camera distance off a bounding box's 3D diagonal badly overshoots for a shape as long and thin as a deck — what actually matters is the box's extent *projected onto the view plane*, not its diagonal; (2) `OrbitControls` recomputes `camera.position` from its own internal state every single frame **regardless of its `enabled` prop** (that prop only gates pointer-input listeners), so `Scene.jsx` skips mounting it entirely when `autoFit` is on rather than merely disabling it — the first attempt at this fought silently with `OrbitControls` and produced an unchanged frame every time, which is why this is called out explicitly. See `public/images/parts/README.md` for the exact regeneration steps if the client ever sends new `.glb` files. **Trucks/Bolts each render as two diagonally-spread clusters — that's correct, not a bad crop.** Unlike a deck or a wheel option, `Board.jsx`'s `Trucks`/`Bolts` are each a *single* mesh whose geometry bakes both the front and rear copies into one buffer (confirmed by dumping every mesh name under `getObjectByName('Trucks')` and finding exactly one) — there's no finer node to isolate one from the other, and cropping to just one would misrepresent the product anyway: buying "Standard Trucks" gets you both. **Follow-up, same day: hardware colour swatches.** The client also asked whether Trucks/Bolts should get a colour picker like `/customize`'s. Explicitly asked and confirmed: **preview-only**, not a real per-colour SKU — there is still exactly one `SkateboardComponent` row each, one price, one stock count; the swatch never touches the `cart.store` payload. Unlike `/customize`, which recolors a *live* Three.js material, `/parts` has no 3D viewer to recolor in real time, so all 11 real swatches (`Components/Customizer/hardwareColors.js` — the one source of truth, not duplicated as a second PHP list) were pre-rendered per hardware piece the same way the base images were (`AutoFitCamera` + the same temporary capture harness, this time also passing `boltsColor`/`trucksColor` through `Scene`, exactly what `/customize`'s own `ColorSwatchPicker` already drives) — `board-{mesh_name}-{hex}.png`, e.g. `board-Trucks-e7312f.png`. `PartController::card()` exposes `mesh_name` + `has_color_variants` (only true for Trucks/Bolts) instead of a fixed `image`; `Parts/Index.jsx` computes the displayed path client-side from the selected swatch and reuses the real `ColorSwatchPicker` component `/customize` already has — same reuse-over-reinvent pattern as everywhere else in this page. **Follow-up, same day: the colour choice now survives into the cart and the order — it is no longer thrown away.** The client's own screenshot of `/cart` showed the gap: picking Red on a Trucks swatch and adding to cart left the cart line looking identical to any other line, with no record of what was picked. Still **not a real per-colour SKU/stock row** (that would need a client decision on whether the shop actually stocks hardware in multiple colours — see the swatch note above) — but now a real *fulfilment note* riding along the line, which is a meaningfully different, smaller claim than either "fake preview, discarded" or "real inventory." Mechanism: `App\Services\Cart::add()` gained a fourth optional param, `?string $color`, stored on the session line exactly like `build_key` (first-write-wins on merge, same reasoning); `AddToCartRequest` validates it as a hex-shaped string (`/^#[0-9a-fA-F]{6}$/`) rather than against the real 11-swatch enum, to avoid a second PHP copy of `hardwareColors.js`. **`OrderItem.customization`** — a JSON column that existed since the very first migration for exactly this ("the 3D customizer's chosen configuration ... colours") but had never once been populated — now finally gets used: `CheckoutController::store()` snapshots `['color' => $line['color']]` onto it. Both `Admin\OrderController::show()` and `Shop\OrderController::show()` expose `item.customization['color']` as `item.color`; `Cart.jsx`, `Admin/Orders/Show.jsx`, and `Storefront/Orders/Show.jsx` all render the same swatch-dot + label (looked up from `HARDWARE_COLORS` — `hardwareColors.js` is a plain data module, shared with the admin panel without violating the admin/storefront presentational-component split, which only bars *styled components*, not utilities). **Scope stayed deliberately narrow**: `/customize`'s own bolts/trucks colour picker is untouched and still never reaches a cart line — that recolour is flavour on an assembled-board preview, not a standalone purchase's fulfilment instruction, and the client wasn't asked about changing that. The caption under the `/parts` swatches was rewritten to match the new reality: "We'll note this colour on your order and try to match it — it isn't a guaranteed stock option." Pinned by `PartBrowsingTest` (colour reaches the cart line, a malformed hex is rejected, no colour leaves the line null) and `CheckoutTest` (colour reaches `OrderItem.customization`, a colourless line gets a null `customization`).
- **Product image upload in the admin: DONE.** Both Create and Edit gained a "Photo" file input (JPG/PNG/WEBP, 4MB cap, client-side preview via `URL.createObjectURL`) — closes the gap the line above used to describe. Uploaded files are saved straight into `public/images/products/` (`Admin\ProductController::storeImage()`), the same physically-served-from-`public/`, not-Laravel's-storage-symlink convention every other product/lookbook image already uses — picked deliberately to stay consistent with how `image_path` is read everywhere else, not the more common Laravel `storage:link` pattern. Replacing a photo on Edit **never deletes the old file** — only swaps which filename `image_path` points at (always `{slug}-{random 8 chars}.{ext}`, the random suffix so a replaced photo never reuses a stale cached filename) — because some product photos are shared with the Home page lookbook (see the Catalogue entry below); deleting on replace could silently break an unrelated page. The Products **Index** table also gained a small thumbnail column (or a grey placeholder square) so the catalogue can be scanned visually instead of by name text alone. **Follow-up, same day:** the new thumbnail column pushed the table wider than a laptop screen, and the "Manage" link scrolled out of reach inside the card's own `overflow-x-auto` — the actual complaint was the button being unreachable, not that scrolling existed at all. Fixed with `sticky right-0` on that one column (both `<th>` and `<td>`, each needing its own opaque background or scrolled-past cells show through), not by shrinking columns — the Manage action is now always reachable regardless of scroll position or screen width. Required enabling the `gd` PHP extension (was off in this XAMPP install) — Laravel's fake-image test helper needs it. 4 new tests (upload on create, non-image rejected, updating without a new file leaves the old one alone, uploading a new file replaces it — using real temp files via `UploadedFile::fake()->image()`, not mocked).
- **Catalogue depth: 39 seeded products** — 12 each of Tees / Hoodies / Caps plus 3 Accessories, via `Product::TYPE_*` (new column, separate from the existing apparel/skateboard `category` — see below). **25 now carry one of the client's real photos** (up from 3, then 18 — see the follow-up at the end of this entry for the most recent +7) — 12 tees, 9 caps, 1 hoodie, all 3 accessories; the remaining 14 apparel slots have `image_path = null` and render the existing grey placeholder block — **not** fake product photography. The client dropped 70 real Facebook-CDN product photos into `public/files/images/`; a subagent catalogued all of them (garment type, colorway, printed text, construction detail, duplicate detection) and matched distinct real designs onto placeholder slots, **renaming the placeholder to match the real design** rather than force-fitting a mismatched name (e.g. "Minimal Wordmark Tee" → "Peace By Plant Tee", "Dad Cap — Khaki" → "Curved Brim Dad Cap" once the real photo showed no khaki colourway exists). **Zero hoodie photos existed in the drop** — all 12 hoodie slots besides the original real one stay placeholder; don't assume a hoodie design exists just because a tee/cap counterpart does. **`Product::TYPE_ACCESSORY` was added afterward** for 3 photos that didn't fit tee/hoodie/cap at all (2 keychains, a canvas strap) — still filed under `category = apparel` (that split is really "sold like apparel," i.e. simple product + variants, vs. the skateboard category's component assembly, not literally "clothing"), seeded with only `color = Black` since that's the only colourway any real photo showed — resist the temptation to pad it out to the Black/White/Olive/Navy spread the other apparel types use just for consistency. New web-ready images went through the existing `tools/lookbook.php` pipeline into the *shared* `public/images/lookbook/` pool (not a separate product-photos folder) — several were already sitting there unused from an earlier lookbook-only upload (`tee-rack-night.jpg`, `syndicate-cap-white-tee.jpg`, `mint-tee-rocks.jpg`, `pink-tee-shoreline.jpg`) and got reused as product photos with zero reprocessing, confirming lookbook and product images are deliberately one shared pool, not two. The other ~52 uploaded originals (duplicates, designs with no open matching slot) are still sitting unsorted in `public/files/images/` — left there deliberately rather than deleted, in case they're useful for hoodie photos or new slots later. `/shop` groups the catalogue into one section per type (`ProductController::index()`), entirely data-driven off `Product::TYPES`/`TYPE_LABELS` — adding `TYPE_ACCESSORY` required zero frontend changes, an "Accessories" section and `?type=accessory` filter just appeared. Admin Create/Edit's Type select is the same story, driven by the same constants. Caps and Accessories both use `size = "One Size"` — the old cap seeder generated S/M/L/XL variants for a cap while its own description said "one size," which is fixed now, not just carried forward. **Follow-up: one colour per product, not a spread — the Colour picker on `/shop/{slug}` is effectively gone.** The client's own screenshots showed the bug: `Product.image_path` is one photo per *product*, not per colour, so picking Black vs. White on "Syndicate Box Logo Tee" never changed the photo — and even "Core Logo Tee — Black" (named for its colour) still offered a White swatch that did nothing. Root cause was `DatabaseSeeder::seedApparel()` giving every product in a type the SAME fixed colour spread (`Black/White` for tees/hoodies, `Black/White/Olive/Navy` for caps) regardless of what the product's own name/photo/description said — structural padding, not real inventory. Client confirmed every real design is genuinely one colour. `seedType()` now takes one colour per catalogue entry instead of one spread per type; each of the 39 products' colour was determined from its name first, its description second, and for the 5 real photos with neither signal (`varsity-hoodie.jpg`, `flame-tee-cap.jpg`, `flame-snapback-cap.jpg`, `brain-cap-black.jpg`, `wordmark-cap-black.jpg`) by actually looking at the image rather than guessing. Two products' old copy (`Flame Graphic Tee`, `Curved Brim Dad Cap`) claimed multiple colours because their real photo is a rack/shelf shot of several colourways together — rewritten to describe the one colour actually being sold. **`Shop/Show.jsx`'s fix is one line**: the Colour `<fieldset>` now renders on `colors.length > 1` instead of `> 0` — the picker mechanism itself is untouched, so if a real multi-colour product is ever added (the admin's variant form already allows arbitrary colours, unchanged), the picker reappears with zero new code. The **dev database** was fixed directly (not `migrate:fresh`, which would have wiped every order/user from this session's testing) via a one-off tinker script: delete every variant whose colour didn't match the chosen one per product, then top up the 11 products whose target colour (Mint, Tie-Dye, Pink, Red, Charcoal, Bone, Grey, Olive-on-a-tee) never existed under the old Black/White-only spread and were left with zero variants. Also added: **`Components/Storefront/SizeChart.jsx`** — a "Size guide" link next to the Size fieldset opening a modal (reusing `Components/Storefront/Modal.jsx`) with generic S/M/L/XL chest/length/sleeve measurements for the oversized/boxy fits the copy already describes. Explicitly generic placeholder numbers, not a fact about this shop's real garments — client confirmed no manufacturer measurements exist yet. Gated inside the component (`type !== 'tee' && type !== 'hoodie'` → renders nothing) rather than at the call site, since Caps/Accessories are "One Size" and a chart doesn't apply to them. **Follow-up: 7 more of the ~52 previously-unsorted originals in `public/files/images/` matched to real slots**, delegated to a subagent given the ~68-photo review volume. Names were changed to fit what the photos actually show rather than forced into the old placeholder identities (e.g. a "SYNDICATE / The Mind Is What Matters" badge tee became **Mind Matters Tee**, in both black and white — two separate products, same pattern as the existing "Core Logo Tee — Black/White" pair, not variants of one). Processed through a one-off PHP/GD script standing in for `tools/lookbook.php` (auto-orient, resize to 1000px, EXIF stripped) since ImageMagick isn't installed on this machine — same output convention, `public/images/lookbook/`, just a different tool on the day it was needed. Left un-filled, deliberately: Beanie, Bucket Hat, and Low-Profile Cap — Navy (no beanie/bucket-hat photos exist anywhere in the drop, and the only navy caps are a near-duplicate of an already-used shelf shot) and all remaining hoodie slots (still zero hoodie photography in the drop, unchanged from above).
- **Shop grid: search, sort, and real derived badges.** `?q=` searches product name (debounced 400ms client-side). `?sort=` is `newest` (default) / `price_asc` / `price_desc` / `popularity` — popularity is real units sold from **paid orders only** (one query for the whole page, joining `order_items → product_variants`, not decorative). `is_new` (listed ≤14 days ago) and `is_limited` (avg <6 units per option) are computed server-side per product — there is no `is_featured`-style flag to fake, so nothing is editorial. **Quick Add** on the grid posts straight to `cart.store` for whichever variant actually has stock (never blindly the first row), the same `back()`-redirect pattern the product page's Add to Cart already used — re-renders self-correct on the next load if that was the last unit.
- **Breeze + Inertia + React + Tailwind: INSTALLED and verified.** Breeze v1.19.2, Inertia 0.6.3, Ziggy.
- **StoreHeader's logged-in state is now a real account dropdown** (`Components/Storefront/AccountMenu.jsx`, headlessui `Menu`) — replaces the old plain "Orders · Name · Log out" inline links, placed after the cart icon (desktop only; the mobile drawer keeps its own flat link list, the correct touch pattern, now with the same Dashboard/Orders/Profile settings/Log out set it was previously missing Orders and Profile settings from). Trigger is an avatar-initial circle + first name; the panel header shows full name + email, then Dashboard/Orders/Profile settings (staff/admin also get Admin panel), then Log out below a divider. **"Profile settings" links to the existing Breeze `profile.edit` page** — password/email changes already live there via `UpdateProfileInformationForm`/`UpdatePasswordForm`, so no new page was needed. **Verified in a real browser with a real click, not just Puppeteer's scripted one** — Headless UI 1.7's `Menu.Button` didn't respond to Puppeteer's native `page.click()`/`page.mouse.click()` at all (`aria-expanded` never flipped), but did respond to a manually dispatched full pointer-event sequence (`pointerdown`/`mousedown`/`pointerup`/`mouseup`/`click`) at the same coordinates — a known Puppeteer-vs-newer-Headless-UI gap (missing synthesized PointerEvents), not a real bug; confirmed working correctly this way for both a customer account (no Admin panel item) and staff (has it), in both themes.
- **Storefront landing page: BUILT.** `/` (route name `home`) now renders `Pages/Storefront/Home.jsx` instead of Breeze's `Welcome` — dark/volt skate-brand look, hero, marquee, masonry lookbook using the client's real photography. Nav's Shop, Orders, cart **and Customize** entries are all live now — none are dimmed "soon" chips any more.
- **`/contact` page: BUILT** (route name `contact`, plain closure in `web.php` alongside `home` — static content, no controller needed yet). `Pages/Storefront/Contact.jsx`: one card per real branch (address + a Google Maps `output=embed` iframe, no API key needed since it just resolves the real address text — not a hand-placed pin) plus the Tagas phone number, then a "Reach us online" row reusing the same real email/Facebook/Instagram/TikTok links the footer has. **The FAQ section is a deliberate, explicit "Coming soon" placeholder** — no invented questions/answers. Nav's "Contact" entry is a real link (not a dimmed "soon" chip) since the route already exists.
- **Phase 5 (skateboard 3D customizer): DONE.** `/customize` — see the dedicated section below. Assets compressed from 136 MB to 8.6 MB and committed. Apparel-in-3D is still blocked on Open decision #1.
- **Tests: 162 passing** (`php artisan test`), including `assertInertia()` checks on every new admin page and on the storefront home — these verify the actual component name and props Laravel returns, the closest thing to a browser check available without one. **The 3D customizer has zero JS test coverage** (no test runner exists in this project) — it was verified by actually rendering it in a headless browser and screenshotting the result, which is how a real, visually-total bug (see the 3D section below) was caught. Re-verify the same way if you touch it.
- **DB works.** `syndicate_ims` (dev, seeded) and `syndicate_ims_test` (tests) both exist.
- **Seeded logins** (password `password`): `admin@syndicate.test`, `staff@syndicate.test`, `customer@syndicate.test`. Staff/admin land straight in `/admin` after login; "View the shop" in the sidebar footer takes them to the storefront.
- **Admin UI direction is locked** (see Design section below): dark throughout — content area now matches the sidebar, which was already dark — one blue accent (still not the storefront's volt green). The sidebar is grouped (Overview / Catalogue / Sales) with icons, and is an **off-canvas drawer below `lg`, static from `lg` up** — the earlier version was a plain `w-60` flex child that ate 240px of a phone screen.
- **Admin panel: DARK theme, follow-up to the icon-button pass — client asked for it to "match the UI and theme of the store."** (Superseded by the later "Control Room" volt re-theme documented near the top of this section — `brand` no longer exists, the admin now shares `volt` with the storefront. Left here for the historical record of what `Card`/`PrimaryButton`/etc. and the icon-button conventions came from.) Explicitly **not** a full storefront reskin — confirmed with the client: dark ground, but keeps its own calmer `brand` (blue) accent rather than the storefront's loud `volt` green, since the admin is still meant to read as a restrained management tool, not a brand surface (the separation CLAUDE.md already documented as deliberate stays true — only *which side of light/dark* the tool sits on changed, not the fact that it's a different visual language from the shop). Two research passes read every admin file before touching anything; findings: `brand` was already a plain alias to Tailwind's stock `blue` scale (no new hex needed), and several patterns were duplicated verbatim 3–20 times across pages (card wrapper ~20×, primary button 4×, table chrome 6×, filter-tabs+pagination between `Orders/Index.jsx` and `Users/Index.jsx`) — extracted into shared components alongside the re-theme rather than editing the same string that many times: **`Components/Admin/{Card,PrimaryButton,SecondaryButton,FilterTabs,Pagination,IconButton,icons}.jsx`** are new. `icons.jsx` is one file of hand-copied Heroicons-outline paths (`viewBox 0 0 24 24`, `strokeWidth 2`, `stroke="currentColor"`) — the exact convention already established storefront-side and by `VariantRow.jsx`'s original pencil/trash pair (now promoted into the shared `IconButton`) — no icon library added. **Color mapping, applied everywhere then a few bespoke exceptions**: `bg-white`/page-root `bg-gray-50` → `bg-gray-900`/`bg-gray-950`, `border-gray-200/300` → `border-gray-800/700`, `text-gray-900/700/600` → `text-white/gray-300/gray-400`, `text-gray-500` unchanged (still reads as muted on dark), `bg-brand-600` primary buttons **unchanged** (blue already pops on near-black). Exceptions: `StockBadge`/`OrderStatusBadge`'s light pill tones (`bg-green-50 text-green-700`) became tinted-dark pills (`bg-green-500/10 text-green-400`) — these stay explicitly outside the `brand` accent, only light/dark treatment changed, not which colors mean what; `Dashboard.jsx`'s Recharts charts needed literal hex (Recharts reads props, not Tailwind classes) — tick/axis colors lightened, and the `Tooltip` previously had **no explicit `backgroundColor`** (defaulted to white — would have rendered as a broken white box on a dark card) now gets one; root `Components/{TextInput,InputLabel,InputError}.jsx` (confirmed via grep: imported only by admin forms, safe to re-theme directly) were fixed to dark **and** to use `brand` instead of a pre-existing off-brand `indigo-500` focus ring `TextInput.jsx` had been using — a real bug the research pass caught, not just a color swap. **Icon-button upgrades beyond the existing pencil/trash**: icon-only for dense/frequent row actions (`VariantRow.jsx`'s Save/Cancel → check/x, pagination's raw `«`/`»` HTML-entity labels → real chevron icons, search buttons → magnifying-glass icon-only and switched off an inconsistent one-off `bg-gray-800` onto the shared `PrimaryButton`); icon **+ kept label** for prominent/infrequent/high-stakes actions where a bare icon would be ambiguous or too casual (`Products/Edit.jsx`'s "Archive product" → archive-box icon, not trash — archiving isn't deleting; `Orders/Show.jsx`'s "Mark fulfilled"/"Cancel order" → check-circle/x; `Users/Index.jsx`'s "Deactivate"/"Activate" → power icon, since the icon alone can't disambiguate the two states). Verified in a real browser across every touched page (Dashboard, Products Index/Create/Edit, Orders Index/Show, Users Index, SkateboardComponents Index/Edit) — confirmed actually-readable contrast, sidebar and content reading as one consistent surface, and every interactive control still works (edited a variant, changed an order status, filtered/paginated orders, viewed the users list) since a pure-CSS/JSX pass can still accidentally break an onClick if a component gets restructured. 162 tests unaffected (purely visual, `assertInertia()` checks component name/props not markup).
- **`/dashboard` routes by role.** Staff/admin are redirected to `/admin`; customers get `Storefront/Account` in the shop's own styling. Breeze's "You're logged in!" page is gone. The route NAME stays `dashboard` because `RouteServiceProvider::HOME` and the auth controllers point at it.
- **Every authenticated page now matches the storefront.** `Profile/Edit` (+ its three partials), `ConfirmPassword`, `VerifyEmail` are all on the dark/volt treatment now. **Breeze's stock `AuthenticatedLayout`, `GuestLayout`, `ApplicationLogo`, `DangerButton`, `SecondaryButton`, `Modal`, `Checkbox` were deleted** — confirmed orphaned (grepped, zero remaining imports) before removal, not just unused-looking. `TextInput`/`InputLabel`/`InputError`/`PrimaryButton` remain, but are now used **only by the admin** (Products Create/Edit) — which is exactly the invariant the admin/storefront JSX-import separation exists for, now true by construction rather than by convention. Storefront destructive actions use the new `Components/Storefront/{DangerButton,GhostButton,Modal}`.
- **Git:** live at `github.com/nixthephdev/syndicate-ims` (private). Active development branch is `feature/storefront`, not `main` — check `git branch --show-current` before assuming which one you're on.
- **3D:** skateboard side integrated and working — see the dedicated 3D section below. Apparel side still has zero models.
- **Storefront light/dark toggle: DONE.** The storefront was dark-only by design; visitors now get a choice. **No CSS variables** — a custom Tailwind `light:` variant (`[data-theme="light"] &`, registered in `tailwind.config.js`) with an explicit `light:` class paired at every dark-mode call site, because `ink-900`/`volt-500` play both an invertible-surface role and a permanently-dark/permanently-loud fill role under the same token, which a variable can't distinguish. Persisted to `localStorage` (`syndicate-theme`) and applied via `data-theme` on `<html>`, set by a synchronous inline script that is the first thing in `app.blade.php`'s `<head>` (before the boot splash) so there is no dark-flash FOUC. Default for first-time visitors is **dark**, deliberately ignoring OS `prefers-color-scheme` — the brand identity (splash, favicon, photography) assumes dark as canonical. Toggle lives in `StoreHeader`'s icon cluster and, absolutely positioned top-right, in `StorefrontAuthLayout`. `resources/js/utils/useTheme.js` (DOM CustomEvent pub/sub, not React Context — only 3 consumers) covers the two spots CSS can't reach: `Scene.jsx`'s Three.js canvas background and `Customize.jsx`'s drei `<Loader>` inline styles. **Admin panel is untouched by this storefront toggle** — it has its own separate dark theme now (no `light:` variant, no toggle — see the admin dark-theme entry above), still deliberately not the same mechanism or palette as this storefront one. See the Design section below for the storefront palette and the deliberately-unpaired exceptions (footer, auth photo panel, lookbook tiles — all photo/scrim treatments, not flat theme-able surfaces).

- **Realistic demo order history for the dashboard charts: DONE, opt-in.** `database/seeders/DemoOrdersSeeder.php` — deliberately **not** called from `DatabaseSeeder::run()`, so a plain `migrate:fresh --seed` still gives the clean 39-product/3-account baseline every test assumes. Run it on top of that baseline whenever the dashboard needs to look like a real store instead of an empty one: `php artisan db:seed --class=DemoOrdersSeeder`. Generates ~90 orders across the last 45 days (15 fake customers via `User::factory()`, real product/component mix, realistic status spread — mostly paid, some awaiting/fulfilled/cancelled), and **every "paid" one goes through the real `InventoryService::commitForPaidOrder()`** — stock genuinely decrements, so low-stock alerts and "top products" reflect what actually "sold," not invented numbers. The only faked thing is the clock: timestamps are backdated across the 45 days via a raw (non-Eloquent) `DB::table('orders')->update()` afterward, since `InventoryService` always stamps `paid_at` with the real `now()` — that real code path is never modified to make this possible. Safe to re-run — only ever adds orders, never touches or deletes existing rows. **This is obviously-fake demo data mixed into real order history** (factory-generated customer names/emails) — fine for showing the panel working, but say so explicitly if anyone asks to see "the real customer list" after this has run.
- **Deployed to Hostinger (shared hosting, collaborator access under `it@pcis.edu.ph`'s account): DONE**, at `https://lightblue-echidna-162258.hostingersite.com` — this is a **shared, multi-tenant account** with three other real, unrelated live domains on it (`m7group.com.ph`, `m7techsolutions.com`, `pcis.edu.ph`); everything for this project stays strictly inside `~/syndicate-ims/` (the app) and `~/domains/lightblue-echidna-162258.hostingersite.com/public_html` (a **symlink** to `~/syndicate-ims/public`, not a real directory — the original placeholder was renamed to `public_html_default_backup`, not deleted). Node.js is **not** installed there — `public/build/` is built locally and uploaded as static files, never built on the server. `deploy/redeploy.ps1` codifies the whole process (build → package as `tar.gz`, **not** `Compress-Archive` — Windows zips store backslash paths that make Linux `unzip` create directories with broken/zero permissions, learned the hard way on the first deploy → upload → `composer install --no-dev` → `migrate --force`, deliberately never `--seed`, since `DatabaseSeeder` has no dedupe guard and would duplicate the catalogue → fix `storage`/`bootstrap/cache` permissions) into one command; it never touches the server's `.env`, so redeploying never disturbs the live DB password or `APP_KEY`. Live DB: `u816959808_synims` (MariaDB). Seeded logins on the LIVE site were deliberately reset to a simple shared password (`password123`, all three accounts) — the client's own call, since this is thesis defense infrastructure, not a real store yet; the same accounts on local dev keep the original seeder default (`password`) — the two are unrelated and were changed independently.
- **Text-link "buttons" → icon buttons, first pass.** Client flagged `Cart.jsx`'s underlined "Remove" as not fitting the UI. **Confirmed convention already established, just extended**: this project hand-copies Heroicons-outline SVG paths inline rather than adding an icon library dependency (`ThemeToggle.jsx`'s sun/moon, `StoreHeader.jsx`'s cart/menu icons, both `ToastStack.jsx`'s dismiss × were already this way) — every new icon here follows the exact same shape (`viewBox 0 0 24 24`, `strokeWidth 2`, `stroke="currentColor"`, `fill="none"`). Converted: `Cart.jsx`'s two "Remove" buttons (single line and the grouped custom-board line) → a shared `RemoveButton` (trash icon, `aria-label`+`title` carry the accessible name since there's no visible text — same pattern `ToastStack`'s dismiss already used); `Admin/Products/Partials/VariantRow.jsx`'s "Edit"/"Delete" table actions → pencil/trash `IconButton`s. **Scanned the rest of the app and deliberately left several as text**, not overlooked: `Admin/Users/Index.jsx`'s Deactivate/Activate (a real word carries more certain meaning here than a toggle icon would), `VariantRow`'s own Save/Cancel pair (already short, an icon wouldn't obviously read faster), `SizeChart.jsx`'s modal Close button (a full-width primary action, not a compact row control), `SkateboardComponents/Index.jsx`'s "Manage →". If more get converted later, reuse `RemoveButton`/`IconButton` rather than hand-rolling a third version of the same trash icon.

## What this project is
Web-Based Retail Management System with Interactive 3D Product Visualization and Inventory Monitoring, for **Syndicate Supply Co.** — an apparel + skateboarding shop in Legazpi, Philippines.

**Three branches, confirmed by the client (2026-08-25):**
- **Purok 1, Tagas, Daraga, 4501 Albay — the MAIN branch** (address from the shop's own Google Maps listing, "Syndicate Supply Co. (Branch 1)"). This is why their Facebook page is titled "Syndicate Supply Co. | Daraga" — an earlier note in this file called that title "likely stale," assuming it contradicted two Legazpi-only addresses. It was correct all along; the assumption was wrong for not accounting for a third, unlisted branch.
- Brgy. 33 PNR, Peñaranda St., Legazpi City
- Pagasa, Rawis, Legazpi City

All three are in the storefront footer, Tagas marked "Main branch," with a
`tel:` link for **0948 211 3209** (from the same Google listing as the
address, sitting under Tagas only — not confirmed to also reach Peñaranda or
Rawis, so it is not presented as a shop-wide number).

**Social links, real accounts from the shop's own Facebook "Contact info" panel** — Facebook (`facebook.com/SSCPHP`), Instagram (`@syndicate_supplyco`), TikTok (`@syndicatesupplycoph`), email (`syndicatesupplyco63@gmail.com`) — four icon links in the footer's brand column (`StorefrontLayout.jsx`), under the tagline. The Instagram/TikTok URLs the client pasted carried Facebook share-tracking junk (`?fbclid=...&mibextid=...`); stripped before use since that's tracking noise, not part of the actual profile URL — the accounts themselves are unedited. No Messenger link — the contact panel shows a Messenger entry but no shareable public link was given for it, so it wasn't invented.

**Hours are still NOT on the site.** The Google listing showed only "Closes
8 PM" with the opening time hidden behind a collapsed dropdown in the
screenshot — publishing a schedule from a half-known closing time is the
same mistake as the invented hours already removed once. Get the full
opening–closing range before adding anything. Same standing rule: never
invent an address, phone number or hours that weren't actually given.

College capstone (STI College Legazpi, BSIT). I'm the freelance full-stack dev building it for the student group; they are the client.

## Environment — exact versions, not "8.x"
| | |
|---|---|
| PHP | **8.0.30** (EOL since Nov 2023) |
| Laravel | 9.52.22 |
| Node | 22.16.0 |
| MySQL | via XAMPP, `root`, no password |
| DB name | `syndicate_ims` |
| OS | Windows 10 + XAMPP |

**PHP 8.0 constrains us:** no enums, no readonly properties, no `never` return type. Use class constants for `role` and order status instead of enums. Blocks Laravel 10+.

Run it:
```
php artisan serve     # http://localhost:8000
npm run dev           # Vite, :5173
```
**Do NOT serve through XAMPP Apache.** XAMPP is only for MySQL.

## Stack — LOCKED
- **Backend:** Laravel 9 + PHP
- **Frontend:** React via Inertia.js, installed through Laravel Breeze (React variant)
- **Build:** Vite · **Styling:** Tailwind
- **3D:** Three.js + @react-three/fiber (pinned `^8`, not `^9` — v9 needs React 19) + @react-three/drei. Installed and in use, not aspirational.
- **Auth/roles:** Breeze + `role` column on users (customer / staff / admin)
- **Payments:** PayMongo, **test keys only**, GCash QR. Deployed live but no real money. Live keys would be the shop owner's own business account — out of scope. See DECISIONS.md.
- **Realtime:** none. ~~Laravel Reverb~~ requires Laravel 11 + PHP 8.2 — **impossible on this stack.** If live stock updates are needed, poll or use Pusher's free tier.

## Conventions — LOCKED
- **Money: integer centavos.** Columns named `*_centavos`, type `unsignedBigInteger`. Never float, never `decimal`. PayMongo's API takes centavos, so this avoids a conversion layer and float drift. Format for display only.
- **Timezone:** `Asia/Manila` (`APP_TIMEZONE` in `.env`, wired into `config/app.php`). Reports are for a PH shop — UTC would be 8 hours off.
- **Stock decrements when payment is CONFIRMED**, not when the order is placed. PayMongo webhook → `DB::transaction()` → re-check availability *inside* the transaction → decrement. The cart does **not** reserve stock. If two shoppers race for the last item, the loser gets a graceful checkout failure. No reservations table.
- **No enums** — PHP 8.0. Use class constants for `role` and order status (e.g. `Order::STATUS_PAID`). Also no readonly properties, no `never` return type.
- Laravel 9 structure: middleware registers in `app/Http/Kernel.php`, *not* the Laravel 11 style.
- Eloquent + Form Request validation. Scaffold with `php artisan make:model X -mcr`.
- **All inventory writes go inside `DB::transaction()`**, with the stock re-checked *inside* the transaction.
- **Tests run against `syndicate_ims_test`** (set in `phpunit.xml`), never the dev DB. MySQL not sqlite — the inventory logic depends on real transaction/row-locking behaviour that sqlite doesn't model. `php artisan test`.

## Open decisions — ASK ME, don't assume
1. **Apparel in 3D — get models from the group, or downscope to 2D images?** See the asset gap below. Blocks objective 1 and needs a paper amendment either way.

## Core objectives (from the proposal)
1. Interactive 3D product visualization — apparel + skateboard assembly; 360° rotate + zoom.
2. Product customization/preview — color pickers, patterns, custom text/graphics. **⚠ see asset gap**
3. Automated inventory monitoring — stock syncs instantly on completed transaction, atomically.
4. Low-stock/restock notification for admins at a critical threshold.
5. Online checkout with GCash QR (PayMongo).
6. Centralized product management (listings, pricing, stock, orders).
7. Role-based access control (customer / staff / admin).
8. Reporting — inventory summaries, order details, sales; printable.
9. Responsive web (desktop + mobile browsers). No native app.
10. Transaction history for admins.

**Out of scope:** native mobile app, AR/virtual fitting, AI/demand forecasting, courier integration, any payment method other than GCash via PayMongo.

## Build order — DO NOT jump to 3D
3D is the flashy part; the *risk* is a half-baked management system at defense. Backbone first.

1. **Data layer** — Product, ProductVariant (size/color/stock), SkateboardComponent, Order, OrderItem. Add `role` to users.
2. **Role-based access** — middleware gating admin routes.
3. **Product management** — admin CRUD.
4. **Storefront** — browse, cart, checkout; atomic stock decrement.
5. **3D customizer — SKATEBOARD SIDE DONE.** R3F, client-supplied models, real DB-backed component list. Apparel side still blocked — see Open decisions.
6. **PayMongo test checkout, reports, low-stock alerts, transaction history.**

## 3D — skateboard customizer: DONE
`/customize` (route name `customize`, load-bearing — see `StoreHeader.jsx`'s nav chip). `App\Http\Controllers\Shop\CustomizeController` + `resources/js/Components/Customizer/{Scene,Board,Wheels,PartPicker}.jsx`. Deck + wheels are picker-driven (14 + 18 real `SkateboardComponent` rows); Trucks/Bolts are included automatically — no picker, since only one active row of each exists in the seeded catalogue. Add-to-cart is one atomic endpoint (`customize.store`) that adds all four as real `Cart` lines — **skateboards are now actually purchasable end-to-end**, which they were not before (the seeded catalogue existed but had zero customer-facing route).

**Assets are compressed and committed**: `public/models/board.glb` (2.46 MB) + `wheels.glb` (6.51 MB), was 136.3 MB combined raw. Full compression pipeline, the sharp/vips crash that blocked gltf-transform's own texture commands on Windows, and the exact mesh-name inventory are documented in [public/models/README.md](public/models/README.md) — read it before touching either file again.

**Home page hero now shows a real, auto-rotating 3D board** (`Components/Storefront/HeroBoard.jsx`) — a fixed decorative showcase (Abstract deck, red BB wheels), not a picker. The ghost "SKATE" watermark text it originally sat in front of was removed once the board became the hero's main visual. **The hero is a real two-column CSS grid now** (`lg:grid-cols-[1.1fr_1fr]` in `Home.jsx`), not text-with-an-absolutely-positioned-decoration-in-the-corner — the board gets its own dedicated column (`min-h-[420px]` → `600px` at `xl`) and is centered against the text column's *actual rendered height* via the grid's `items-center`, which an absolutely-positioned `top-1/2` box relative to the whole variable-height section could never guarantee. `HeroBoard.jsx` itself no longer sizes or positions itself at all (`absolute inset-0`, fills whatever box it's given) — that separation is deliberate: a component that insists on its own fixed px dimensions can't be handed a bigger or smaller slot later without editing it. Three layered effects give it depth rather than reading flat: two blurred volt-green (`#ccff00`) circles at different sizes/opacities (big soft outer + tighter bright inner, not one flat blob) plus a matching volt-coloured `pointLight` inside the scene (`Scene.jsx`'s `accentColor` prop) for the neon look; a blurred dark ellipse under the board's resting position so it reads as floating above a surface, not pasted on the background; and `animate-float` (a new keyframe in `tailwind.config.js`, slow vertical drift) layered on top of the model's own `autoRotate` — two independent motions read as "alive," either alone reads as "spinning in place" or "static." **`accentColor` is deliberately opt-in, `null` by default** — the actual `/customize` picker never passes it, so a shopper judging a real deck's colour before buying never sees it tinted green. Verified across three viewport widths (1100/1600/1920px, including the tightest `lg`-only case) in a real headless browser — properly centered and no text/board collision at any of them. **Code-split on purpose**: `Scene.jsx` is dynamically `import()`-ed via `React.lazy` in `HeroBoard.jsx`, not statically imported, because Three.js + R3F previously only shipped on `/customize` (~900KB) — importing it directly into `Home.jsx` would have doubled the bundle size of the site's highest-traffic page. Vite/Rollup automatically factors the shared dependency into its own `Scene-*.js` chunk since it's now both statically imported (`Customize.jsx`) and dynamically imported (`HeroBoard.jsx`) — `Customize.js` itself shrank from ~911 KB to ~8.6 KB as a result, a net win for that page too. `Scene.jsx` also gained `transparentBackground`/`autoRotate`/`enableZoom`/`enablePan` props (all default to the old behavior) so the same component serves both the functional customizer and this decorative hero without a fork. **Verified in a real (headless) browser via a temporary Puppeteer install** (`npm install --no-save puppeteer`, removed after use — not a project dependency) — this caught and confirmed the fix for a real bug: **`Board`/`Wheels` now each clone `useGLTF`'s cached scene once per mount (`cachedScene.clone(true)`)**. `useGLTF` caches the parsed GLTF by URL for the whole SPA session, and Inertia never does a full page reload — so the Home hero showcase and the `/customize` picker, despite being "different pages," were mounting the SAME cached `board.glb`/`wheels.glb` scene graph and mutating it in place (visibility flags, `Wheels.jsx`'s `centerPivotAndReturn` reparenting, bolts/trucks material colour). Whichever mounted second inherited the first's mutated state, or fought over reparented pivots — symptom: wheels missing or wrong after navigating between the two pages client-side. Confirmed fixed in both navigation directions (customize→home and home→customize) with a real SPA nav (not a hard reload, which would have masked the bug). The existing per-mesh material clone-once guard (`__syndicateCloned`) still works correctly on top of this — it's keyed off the *original* shared material, which the per-mount clone never mutates directly, so each mount still independently clones its own material the first time through.

**One non-obvious real bug, caught only by actually rendering in a browser and comparing decks side by side**: every deck's mesh has TWO primitives sharing one node — griptape (generic, identical material on all 14 decks) and the printed graphic (the actual per-deck texture) — and the graphic is on the *underside*. A camera looking down at the board shows griptape, identical regardless of selection. Fixed by pointing the default camera up from below (`Scene.jsx`), matching what the reference demo's own hardcoded board camera already did for the same reason. **This could not have been caught by `php artisan test`** — there is no JS test runner in this project, and the bug was 100% visual. If you touch camera positioning or the deck mesh-toggle logic again, verify with an actual rendered screenshot, not just a clean build + passing PHP suite.

**Hover-to-preview (added, needs a manual browser check before trusting it):** hovering/focusing a Deck or Wheels option in `PartPicker.jsx` now live-updates `Scene.jsx` to show that actual mesh, via a `previewDeckId`/`previewWheelsId` state pair in `Customize.jsx` kept fully separate from the real `deckId`/`wheelsId` selection — the viewer shows `preview ?? selected`, and only a click ever changes the real selection or the price. Cheap by construction: `Board`/`Wheels` already just flip `.visible` on the one already-loaded scene graph on prop change, so hovering doesn't reload or re-suspend anything. **Not yet verified by actually rendering it** — this project's own standing rule (see the griptape-camera bug above) is that mesh-toggle-logic changes need a real screenshot check, not just a clean build, and no browser tool was available when this was written.

**Bolts/Trucks color: DONE, and it's a genuinely different mechanism from Deck/Wheels.** `Components/Customizer/ColorSwatchPicker.jsx` + `hardwareColors.js` (11 swatches, hex values copied verbatim from `reference/skate-demo/index.html`'s `bolts_color`/`trucks_color` radio inputs, not invented). Unlike Deck/Wheels — which swap between separately-priced `SkateboardComponent` rows — this is a **free live material recolor** of the one fixed `Bolts`/`Trucks` mesh (`material.color.set()` in `Board.jsx`, ported from `main.js`'s `updateMaterialColor()`), because the seeded catalogue only has one row of each and there's nothing to swap between. It never touches `totalCentavos` or the `customize.store` payload — purely cosmetic, no SKU, no stock. This is the first non-inventory-backed customization in the project; flagged explicitly here so it isn't mistaken for the same kind of choice Deck/Wheels are.

**Still true, unchanged:**
- **Customization is mesh-visibility toggling over baked variants**, not free assembly with arbitrary colors.
- **No bearings.** No separately swappable grip tape (grip is only a roughness tweak on meshes named `grip`/`tape`/`top`/`sand`).
- **No apparel models at all** — objective 1 promises apparel in 3D. Open decision #1, still unresolved.
- **Objective 2's "custom text/graphics" has zero asset support.** Decals on baked-texture boards is real work. Flag or cut.
- Reference loader: `reference/skate-demo/main.js` (loads root-absolute `/board.glb`, `/wheels.glb`). Folder is intact and still runs standalone: `cd reference/skate-demo && npm install && npx vite`. It is the ground truth for the mesh-toggle logic — `Board.jsx`/`Wheels.jsx` are a faithful translation of it, not a reinterpretation.

## Design — admin panel
Two different UI jobs, sharing a palette but not a skin:
- **Admin/staff backend** (built): the "Control Room" look — dark throughout (`ink-950` page root, `ink-900` sidebar/cards/topbar), **volt** (`#ccff00`) as its accent, Oswald (headings/stat figures) + Plus Jakarta Sans (body/UI), sharp `rounded-md` (6px) corners, dense restrained tables. This used to be a separate calmer blue accent (`brand`, now deleted) — the client first asked the admin to "match the store," then approved a bolder mockup and asked for it applied everywhere. **`volt` and `ink` are the same tokens the storefront uses, same hex values** — but still a genuinely different execution: Oswald/Jakarta not Anton, sharp small corners not loud full-bleed photography, dense restrained tables not a brand voice, and **zero storefront JSX components imported** — that import boundary, not the hex value, is the real separation. If an admin component shows up in a storefront import (or vice versa), something has drifted.
- **Customer storefront** (landing page built): bold, high contrast, skate-brand voice. Near-black ground (`ink` scale), the same loud `volt` accent, and Anton as the display face (`font-display`) — Anton/full-bleed photography/marquees is what actually keeps this from reading like the admin, not a different colour token. Shell is `Layouts/StorefrontLayout.jsx`; it deliberately imports nothing from the admin.
  - **Lookbook photos are REAL now** — the client's own shots off their Facebook page, no more picsum. Originals in `public/files/images/lookbook-src/`, web copies in `public/images/lookbook/`, wired into `Pages/Storefront/Home.jsx`.
  - **Never hand-edit `src`/`ratio` in the LOOKBOOK array.** Drop new photos in `lookbook-src/` and run `php tools/lookbook.php` — it resizes to 1000px, strips EXIF, backs off quality if a re-encode would grow the file, and prints the array to paste. It strips EXIF because **phone photos carry the shop's GPS coordinates**; publishing those is not something to do by accident.
  - Keep a mix of tall/square/wide — the varied ratios are what make the masonry stagger. Nine identical ratios render as a plain grid.
  - Tiles are **full colour, not grayscale-until-hover**. That was the placeholder-era treatment; the shop sells mint/pink/red apparel shot on Albay beaches, and desaturating it hides the product.
  - `Components/Storefront/LookbookTile.jsx` still falls back to a generated block if an image fails, so a flaky connection at defense won't render broken-image icons.
  - **Storefront-styled auth: Login, Register, ForgotPassword, ResetPassword** (`Layouts/StorefrontAuthLayout.jsx`, split screen with the shop's doorway photo). They use `Components/Storefront/FormControls.jsx` — **not** Breeze's `TextInput`/`InputLabel`/`Checkbox`, which are now the admin's own dark-themed form controls (re-themed alongside the admin dark pass — see the Current state entry). Never "unify" the two sets; that is the coupling the separate `volt`/`brand` tokens exist to prevent. (Root `Components/PrimaryButton.jsx` specifically is unused by either side — confirmed via grep — left alone.)
  - **Loading states, three layers.** (1) Boot splash — logo + volt bar, markup and *inline* CSS in `app.blade.php`, dismissed by `app.jsx` after first paint. Inline because the compiled stylesheet has not arrived yet; a Tailwind-classed splash flashes unstyled. It is gated on the Inertia component name to customer pages only, pinned by `StorefrontTest::test_boot_splash_shows_on_customer_pages_only` — **the admin gets no brand splash**. Carries a 6s JS failsafe and a `<noscript>` hide so it can never trap a visitor. (2) Inertia's top progress bar, volt, `delay: 250` so fast navigations show nothing. (3) `animate-fade-up` on storefront page content, replayed per visit because Inertia remounts.
  - **The splash is not artificially delayed.** Locally React mounts in ~50ms so you will barely see it; that is correct. To actually watch it, throttle the network in devtools. Do not add a minimum display time — it makes the site slower to look busier.
  - **All 6 Auth pages (`Login`, `Register`, `ForgotPassword`, `ResetPassword`, `ConfirmPassword`, `VerifyEmail`) are on `StorefrontAuthLayout`** — none are left on Breeze's `GuestLayout` any more.
  - **Light mode.** The storefront is no longer dark-only — a `light:` Tailwind variant (`plugin(({ addVariant }) => addVariant('light', '[data-theme="light"] &'))` in `tailwind.config.js`) is paired at every dark-mode class site, chosen over CSS variables because `ink-900`/`volt-500` each play both an invertible-surface role and a permanently-dark-or-loud fill role under one token name — a variable can't tell those apart, a paired class always says which one it means. Palette: page ground `#f7f6f2` (`paper`), panel `#efeee6` (`paper-panel`), accent stop `#e2e0d4` (`paper-accent`), primary text `ink-900` (~17.4:1). A new stop, **`volt-800: #5c7600`**, exists solely for `text-volt-500` used directly on page ground (raw volt there is ~1.2:1, illegible) — never used as a fill. Muted-text/border alpha is NOT a straight mirror between themes; light backdrops dilute dark foregrounds faster (WCAG relative luminance), so `light:` pairs generally run ~15 alpha points hotter than their dark equivalent. **Fill-role color (`bg-volt-500`/`bg-amber-400` + `text-ink-900` — buttons, badges, chips, the marquee, Home's closing CTA) is deliberately left unpaired**, same for `Cart.jsx`'s Shop/Index.jsx's `Badge` "ink" tone and its quick-add chip (photo-overlay legibility, not theme-dependent) — pairing it would blunt the one thing meant to stay loud and legible in both themes. **Photo+scrim treatments are deliberately unpaired too**: the footer (also blocked on `logo-full.png` having no `-dark` counterpart — see the Logo section), `StorefrontAuthLayout`'s `<aside>` brand panel, and `LookbookTile.jsx`. Persisted to `localStorage` (`syndicate-theme`); a synchronous inline script — first thing in `app.blade.php`'s `<head>`, before the boot splash — sets `data-theme` on `<html>` pre-paint so there's no dark-flash FOUC. **Default is dark for first-time visitors, OS `prefers-color-scheme` is deliberately ignored** — the whole brand identity (splash, favicon, photography) assumes dark as canonical, and most devices default light. Toggle: `Components/Storefront/ThemeToggle.jsx`, placed in `StoreHeader`'s icon cluster and absolutely top-right in `StorefrontAuthLayout`. Two spots are unreachable by CSS and use `resources/js/utils/useTheme.js` (DOM `CustomEvent` pub/sub, not React Context — only 3 consumers total) instead: `Scene.jsx`'s Three.js `<color attach="background">` and `Customize.jsx`'s drei `<Loader>` inline styles. **Admin panel now has its OWN light/dark toggle, a deliberately separate mechanism from this one** — different attribute (`data-admin-theme` vs `data-theme`), different variant (`admin-light:` vs `light:`), different localStorage key, different DOM node carrying the attribute (a thin wrapper inside `AdminLayout`, not `<html>`). See the Current state entry for the full breakdown, including a real descendant-selector bug it surfaced.

**Why red/amber/green stay literal, not volt, for status:** `DangerButton` and `OrderStatusBadge`/`StockBadge` already own red for destructive/critical, amber for warning, green for healthy/fulfilled. `OrderStatusBadge`'s `paid` status is deliberately literal blue rather than volt too — a status colour must never be mistaken for "this is a clickable action," which is exactly what volt now means everywhere else in the admin (buttons, active nav, links).

If the group supplies real brand colors later, swap `volt` in `tailwind.config.js` — both the admin and storefront read from that one token, nothing is hardcoded per-page.

### Logo — the one real brand asset
Client supplied a white line-art brain + ™ mark. **Source of truth: `public/files/images/logo.jpg`** (960×960 JPEG, mark sits off-center in a large black field). Derived web assets in `public/images/` were generated from it with ImageMagick — regenerate from the source, never from a derivative:

| File | What | Use on |
|---|---|---|
| `logo-mark.png` | brain only, white, transparent | dark surfaces only |
| `logo-mark-dark.png` | brain only, ink, transparent | light surfaces (print, anywhere light-mode still applies) |
| `logo-full.png` | brain + ™, white, transparent | dark surfaces |
| `favicon.ico` (16/32/48) + `images/favicon-*.png` + `apple-touch-icon.png` | brain only, recolored volt (`#ccff00`), transparent background | browser chrome |

Non-obvious constraints, all learned the hard way:
- **The source is JPEG, so it has no alpha.** Transparency was reconstructed by using the image's own luminance as an alpha mask (`-level 22%,72%` to kill JPEG noise in the black field without eating anti-aliased stroke edges). Dropping the `.jpg` straight into a page puts a black square on the design.
- **Exact geometry in the source — do not re-measure by eye, and never by cropping a percentage of the canvas.** Verified with `-connected-components 8`:
  - brain outline = `435x334+240+313` (spans x 240–675)
  - ™ glyphs = two blobs at x 633–720, y 318–364 — the **T overlaps the brain's x-range**, so cropping the brain's bbox drags in a stray "T". Blacken `rectangle 626,310 726,372` first; that rect provably contains only ™ (2176 white px = T's 669 + M's 1507, zero brain pixels).
  - full mark incl. ™ = `480x334+240+313`
  - A first attempt measured "brain only" by cropping the left 63% of the canvas and trimming. That silently clipped 70px off the right lobe — the crop window truncated the very bbox being measured, and the flat-edged result shipped before it was spotted. Measure with connected-components, then eyeball the render on a dark background.
- **`logo-mark.png` is white — it is INVISIBLE on white.** That is why the dark variant exists. Check the surface before picking one.
- **Favicon is now transparent-background, brain-only, recolored volt (`#ccff00`)** — the earlier ink-on-volt-square version (baked background, no transparency) was replaced per client/dev request; the volt tint reads on both light and dark browser tab bars, which plain white (see `logo-mark.png` above) would not. Regenerated from `logo-mark.png`'s alpha channel (`-alpha extract` → recolor a solid volt canvas via `-compose CopyOpacity`, not `-level-colors`, which silently zeroed the alpha channel when tried directly on the RGBA source) — never re-bake a background square without being asked again.
- **At 16px the line-art degrades to a blob** — inherent to strokes this fine; nothing to fix short of a simplified small-size mark. 32px (what modern browsers mostly use) is clean.

## Data layer — the rules that matter
- **The cart (`App\Services\Cart`) is session-backed and reserves NOTHING.** Prices are read live on every render, never cached into the session — a cart can sit for days and charging a stale cookie price is a real bug. Snapshotting happens once, onto `OrderItem`, at checkout. Lines whose variant was archived meanwhile are silently pruned. Anything the cart says about stock is advisory and already stale; `InventoryService` behind its row lock is the only real answer.
- **Cart item types are an allow-list of aliases** (`variant`, `component` → `Cart::PURCHASABLE_TYPES`), never class names from request input. A test pins this.
- **`InventoryService` is the only place ORDER-DRIVEN stock decrements happen.** Nothing in the checkout/payment path may touch a `stock` column except through it — it marks paid and decrements in one transaction, locks each row, re-checks inside the lock, aggregates duplicate lines, and no-ops on a repeat webhook. Admin CRUD (restocking, correcting a count) is a separate, legitimate direct write — see `Admin\ProductVariantController`. The rule is about the *concurrency-sensitive* path, not every write to the column.
- **`role` is NOT in `User::$fillable`** — deliberately. Breeze's `/register` mass-assigns request input, so a fillable `role` would let anyone POST `role=admin` and self-promote. There's a test pinning this (`RoleAssignmentTest`). Assign roles explicitly.
- **Role middleware is hierarchical**: `role:staff` admits staff *and* admin. Unknown roles fail closed.
- **Order lines are polymorphic** (`purchasable` → ProductVariant | SkateboardComponent), and snapshot `name_snapshot` + `unit_price_centavos`. Never render an order from live product records.
- `ProductVariant.price_centavos` is nullable — null means inherit `Product.base_price_centavos`.

## Layout — non-obvious directories
| Path | What | In git? |
|---|---|---|
| `reference/skate-demo/` | The group's original Three.js demo, intact and runnable. Reference only, deliberately outside the web root. | source yes, `.glb` no |
| `public/models/` | Compressed, web-ready `board.glb` + `wheels.glb`, actually served to `/customize`. | yes |
| `public/draco/` | Self-hosted Draco decoder (copied from `node_modules/three/examples/jsm/libs/draco/`) — not the Google CDN, for defense-day reliability. | yes |
| `docs/client/` | The group's capstone paper + client documents. | no |

## Housekeeping owed
- The group must fix their paper: Technical Background says Node.js, Table 6.0 says PHP. It's Laravel/PHP. The panel compares doc against live system.
- **Back up `reference/skate-demo/public/*.glb` outside the project.** Git-ignored — GitHub is not protecting them.

## Deployment
**Live at `https://lightblue-echidna-162258.hostingersite.com`** (Hostinger shared hosting), PayMongo in test mode. See the Current State entry above for the real details — shared multi-tenant account, `public_html` symlink, no Node on the server, `deploy/redeploy.ps1` for updates. Keep 3D scope tight so it doesn't starve the inventory/order modules.
