APP_NAME="Syndicate IMS"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_TIMEZONE=Asia/Manila

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=syndicate_ims
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# OTP codes (registration, login 2FA, checkout) are emailed, so mail MUST work
# or those flows dead-end. `log` writes the whole email — code included — to
# storage/logs/laravel.log, which is the right local default: this is a
# Windows/XAMPP project with no mailpit container to talk to.
#
# For the live site, switch to real SMTP. Gmail example (needs an App Password
# from a 2FA-enabled Google account, NOT the normal account password):
#   MAIL_MAILER=smtp
#   MAIL_HOST=smtp.gmail.com
#   MAIL_PORT=587
#   MAIL_USERNAME=syndicatesupplyco63@gmail.com
#   MAIL_PASSWORD="your-16-char-app-password"
#   MAIL_ENCRYPTION=tls
#   MAIL_FROM_ADDRESS="syndicatesupplyco63@gmail.com"
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@syndicatesupply.test"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false


# Only set this on a host with no SSH access — see DeployController.
# Leave empty everywhere else; an empty value disables the route (404).
DEPLOY_TOKEN=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Where customers are told to send GCash / bank transfers, shown on the order
# page before they upload a receipt. See config/shop.php.
#
# The defaults are OBVIOUS PLACEHOLDERS and the storefront shows a visible
# "don't send money to these" warning while SHOP_PAYMENT_DETAILS_REAL is
# false. Set it true only once the real accounts below are filled in.
SHOP_PAYMENT_DETAILS_REAL=false
SHOP_GCASH_NAME="Syndicate Supply Co."
SHOP_GCASH_NUMBER="09XX XXX XXXX"
SHOP_BANK_NAME="Bank name"
SHOP_BANK_ACCOUNT_NAME="Syndicate Supply Co."
SHOP_BANK_ACCOUNT_NUMBER="XXXX-XXXX-XXXX"
